# Copy this file to "secrets.toml" (in the same .streamlit folder)
# and replace the placeholder below with your real Gemini API key.
# NEVER commit the real secrets.toml file to GitHub.

GROQ_API_KEY = "ENTER_YOUR_GROQ_API_KEY_HERE" 

